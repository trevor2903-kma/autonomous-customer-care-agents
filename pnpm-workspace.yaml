# pnpm workspaces — chỉ gồm package JS/TS. Backend (apps/backend) là Python (uv), KHÔNG trong workspace.
# Mobile (Expo) NẰM trong workspace nhờ node-linker=hoisted (.npmrc) + React đồng bộ 18.2.0 toàn repo.
packages:
  - "apps/dashboard"
  - "apps/mobile"
  - "packages/*"
